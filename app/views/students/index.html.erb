<h1>These are some cool students:</h1>

<ul>
  <% @students.each do |student| %>
    <li> <%= student.name %> </li>
    <li> <%= student.mod %></li>
    <li> <%= student.duck %></li>
  <% end %>
</ul>
